# Envision Library Audit — Final Pre-Release, v0.17.0 (2026-07-25)

**Final pre-release audit** before the v0.17.0 crates.io publish. Graded against main at commit `2a5e5a7` (working tree clean). Target release: **v0.17.0**.

```
==========================================================================
ENVISION LIBRARY AUDIT REPORT
Date: 2026-07-25
Version: 0.16.0 (v0.17.0 pre-release, main @ 2a5e5a7)
Commit: 2a5e5a7
Test Results: 7306 unit / 2591 doc / 7466 integration (17,363 total, 0 failed, 10 ignored)
==========================================================================

OVERALL GRADE: A (3.95 weighted GPA)

EXECUTIVE SUMMARY: All three defects that held the prior audit at 3.90 are
verified closed on main: CHANGELOG/MIGRATION are split to 356/224 lines with
linked legacy archives (PR #510), the 26 redundant ratatui imports are gone
from examples with only legitimate concrete-widget imports remaining (PR
#511), and the duplicate README Display Components table is merged into one
coherent table (PR #512). The scorecard is 9/9, the suite is 17,363 green
tests with 0 clippy warnings and 0 unsafe blocks, and no new findings
surfaced this pass. Remaining weaknesses are all known, cosmetic, and
scheduled (Cadence D aliases, naming outliers, snapshot concentration) —
nothing blocks the release.

GRADE BREAKDOWN:
--------------------------------------------------------------------------
Group 1: First Impressions (15%)                          GPA 4.00
  1.  Getting Started .............. A   Duplicate table fixed; doc-tested README, 14-flag table; only release-checklist version pin + AppShell row remain
  2.  Examples ..................... A   91 examples, 100% coverage, all compile; remaining 22 ratatui imports are all legitimately-not-in-prelude widgets
  3.  Documentation ................ A   100% doc-test coverage on 1777 pub fns, missing_docs enforced, 232 # Examples sections

Group 2: API Design (25%)                                 GPA 3.88
  4.  Consistency & Symmetry ....... A-  selected_item() canonical, 0 accessor gaps, 73/73 derives; 17 undeprecated selected() aliases (Cadence D)
  5.  Modularity & Composability ... A-  14 feature flags, minimal leakage (6 deliberate sites); tokio remains mandatory
  6.  Usability & Ergonomics ....... A   Complete prelude, instance methods, typestate builder turns missing args into compile errors
  7.  Complexity Hiding ............ A   MessageSender<M> newtype holds; prelude ships first-party layout/style types; no cfg(test) gating
  8.  Type Safety & Errors ......... A   Structured EnvisionError, first-party TrySendError/MessageSendError, trybuild compile-fail tests

Group 3: Engineering Quality (20%)                        GPA 4.00
  9.  Algorithms & Data Structures . A-  Alloc-tracking benches; compact_str adoption still sporadic (commit-or-drop decision pending)
  10. Smart Library Usage .......... A+  0 unsafe, 0 clippy suppressions in 195K lines, every heavy dep optional and gated
  11. Performance & Benchmarking ... A   6 criterion suites incl. 1000-item view + alloc benches, run in CI with reports

Group 4: Testing (20%)                                    GPA 4.00
  12. Unit Testing ................. A-  4316 component tests; per-component distribution uneven; snapshots concentrated in ~20/74 (N7)
  13. Integration & E2E Testing .... A   11 suites: proptest x2, stress (10K), async, serialization, trybuild
  14. Doc Test Coverage ............ A+  2591 doc tests, 100.0% of public fns — best in class

Group 5: Architecture (10%)                               GPA 3.90
  15. Solving Pain Points .......... A   Headless testing first-class; 6 built-in themes, custom themes via public Theme
  16. Code Organization ............ A-  CHANGELOG/MIGRATION split landed (356/224); 522 flat re-exports + 5 files at 980-997 lines remain watch items
  17. Extensibility ................ A   Custom components/themes/subscriptions first-class; disciplined CHANGELOG + semver

Group 6: Missing Pieces (10%)                             GPA 3.85
  18. Feature Flags ................ A   Component-group opt-outs, README documents every flag; dedicated no-default-features CI job
  19. Error Infrastructure ......... A-  Structured hierarchy; only 33 # Errors sections, no error-handling guide
  20. Logging & Debugging .......... A-  tracing feature, 28 call sites; not full event-flow tracing
  21. Guides & Migration ........... A   MIGRATION v0.16->v0.17 covers all 6 breaks with before/after tables; legacy archive linked
  22. Advanced Features ............ A   Clipboard, undo/redo, search/filter, lifecycle hooks all present
  23. Serialization ................ A   serialization feature + load_state + dedicated 320-line test suite
  24. Security ..................... A-  SECURITY.md, ANSI parser isolated + tested; no escape-sequence fuzz target
  25. Ecosystem Integration ........ A-  Idiomatic ratatui, DualBackend; hard-wired to tokio, pinned ratatui major
--------------------------------------------------------------------------

Weighted GPA: 4.00(.15) + 3.88(.25) + 4.00(.20) + 4.00(.20) + 3.90(.10) + 3.85(.10) = 3.95 -> A
```

## Changes vs 2026-07-05 post-cleanup audit (A, 3.90 @ 4f3bd06)

| Category | Prior | Now | Why |
|---|---|---|---|
| 1. Getting Started | A- | A | PR #512 merged the duplicate `### Display Components` tables into one (single occurrence at README.md:262, descriptions reconciled). N1 CLOSED. |
| 2. Examples | A- | A | PR #511 dropped the 26 redundant imports; verification: 22 `use ratatui::` lines remain across 22 files, every one `ratatui::widgets::{Paragraph, Block, Borders, ...}` — concrete widgets the prelude deliberately does not re-export. |
| 16. Code Organization | B+ | A- | PR #510 landed: CHANGELOG.md 1469 → 356 lines, MIGRATION.md 1265 → 224 lines, both with explicit links to `CHANGELOG-legacy.md` / `MIGRATION-legacy.md`. The active-document 1000-line violation is gone. Held at A- (not A) for the 522-item flat re-export namespace and 5 source files in the 980–997 band (N6). |

All other 22 categories held. Net: 3.90 → 3.95.

## Verification of closure targets

1. **Category #16 / doc split (PR #510):** `wc -l` confirms CHANGELOG.md 356, MIGRATION.md 224. CHANGELOG.md:356 links `CHANGELOG-legacy.md` (v0.14.x and earlier); MIGRATION.md:224 links `MIGRATION-legacy.md` (v0.13.x and earlier). Active CHANGELOG retains `[Unreleased]` (188 lines of v0.17 content including the Known Deferred Findings block at CHANGELOG.md:188) plus 0.16.0/0.15.1/0.15.0. Active MIGRATION covers v0.16→v0.17, v0.15→v0.16, v0.14→v0.15. **CLOSED.**
2. **Category #2 / example imports (PR #511):** 22 remaining `use ratatui::` lines (down from 46), one per file across 22 of 91 examples, all importing concrete widgets (Paragraph in all 22; Block/Borders/BorderType/Padding/List/ListItem in a subset). Zero redundant-with-prelude imports remain. `cargo build --examples --all-features` PASS. **CLOSED — remaining imports are correct, not defects.**
3. **Category #1 / README dedup (PR #512):** exactly one `### Display Components` heading (README.md:262); the merged table carries the reconciled descriptions (e.g. ProgressBar: "Progress display with ETA and rate"). **N1 CLOSED.**
4. **Findings #6 and #8 (consistency-cleanup cadence):** still closed. `selected_item()` present in 16 component files; `AppHarness::message_sender()` returns `crate::harness::MessageSender<A::Message>` (src/harness/app_harness/mod.rs:272) wrapping the tokio sender (src/harness/message_sender.rs:42), with `MessageSendError`/`TrySendError` re-exported from `harness` in lib.rs. No regression.

The full diff `4f3bd06..2a5e5a7` contains exactly the three PRs (doc split, example imports, README dedup + audit-report check-in) — no unrelated drift.

## Trust-Eroding Findings (ranked by severity)

1. **[OPEN — release-checklist, not a defect] README pins `version = "0.17"`** (README.md:47) while Cargo.toml is still 0.16.0. Unresolvable until the `/release minor` bump ships; the release checklist must verify README snippets against the post-bump Cargo.toml. (N2; affects category 1.)
2. **[DEFERRED — Cadence D, v0.18+] 17 components carry an undeprecated `selected()` alias** for `selected_index()` alongside the canonical `selected_item()` (verified: 17 files with `pub fn selected(`). Documented in CHANGELOG's Known Deferred Findings block. (Affects category 4.)
3. **[DEFERRED — v0.18+] Naming outliers:** `tab_bar::set_selected_index()`, `checkbox::is_checked()` beside `checked()`, `gauge::label_text()`. (N3; affects category 4.)
4. **[DEFERRED — v0.18+] `restore_terminal` renamed to `restore` at crate root** — the tool's single REEXPORT_GAP. (N4; affects category 4.)
5. **[DEFERRED — ride with next README pass] `AppShell` still listed in the README Utility table** (README.md:332) while living in `src/layout/`, outside the 74-component count. (N5; affects categories 1, 3.)
6. **[WATCH — v0.18+] 5 source files in the 980–997 band** (text_area/tests.rs 997, loading_list/mod.rs 993, file_browser/mod.rs 987, terminal_output/ansi.rs 981, pane_layout/tests.rs 980) — headroom nearly exhausted. (N6; affects category 16.)
7. **[WATCH — v0.18+] Snapshot coverage concentrated in ~20/74 components;** conversation_view/log_correlation/log_viewer/multi_progress report 14 tests each. Verify tool undercount vs real gap. (N7; affects category 12.)
8. **[DEFERRED — commit-or-drop decision pending] compact_str adoption is sporadic** — `CompactString` appears in only 2 non-test source files (src/component/cell.rs, src/backend/cell/mod.rs) against a crate-wide dependency with serde wiring. Either adopt it across string-heavy state or drop the dependency. (Affects category 9.)
9. **[FOOTNOTE — accepted resolution] The legacy archives themselves exceed 1000 lines** (CHANGELOG-legacy.md 1124, MIGRATION-legacy.md 1048). These are frozen, append-never historical records created exactly as the prior audit's improvement #1 prescribed, and the 1000-line scorecard rule counts `.rs` files. Noted for honesty; no action recommended. Not counted against category 16.

No NEW findings surfaced this pass. Every open item above is known-and-scheduled (or, for #9, an accepted consequence of a prescribed fix). **Nothing is a v0.17.0 release blocker.**

## Top 5 Improvements (highest ROI)

1. **During `/release minor`: verify README.md:47 resolves post-bump** — `grep 'version = "' README.md` against Cargo.toml after the version bump, before `cargo publish`. Retires N2 permanently.
2. **Add `#[deprecated(note = "use selected_index()/selected_item()")]` to the 17 `selected()` aliases** (Cadence D) — category 4 from A- to A; turns the backlog into a compiler-guided consumer migration.
3. **Fix the three naming outliers + drop the AppShell row or footnote it as a layout utility** (N3/N5) — small, rides with any v0.18 doc pass; lifts categories 1/3/4 residue.
4. **Make the compact_str commit-or-drop decision** — category 9 from A- to A. Two files do not justify a crate-wide dependency with a serde feature knob.
5. **Split the 5 files in the 980–997 band before they breach** (N6) and broaden snapshot coverage beyond ~20 components (N7) — protects categories 12/16 before the v0.18 cycle adds code.

## Detailed Findings

### Group 1: First Impressions — GPA 4.00

**1. Getting Started — A** (was A-)
- README (392 lines) states purpose in the first sentence; badges for CI/coverage/docs/crates/license; doc-tested via `#[doc = include_str!("../README.md")]` so the quick start cannot silently rot (2,591 doc tests PASS).
- Feature Flags section (README.md:23–48) documents all 14 flags with defaults and an opt-out example.
- Duplicate Display Components table fixed (single heading, README.md:262, reconciled descriptions).
- MSRV 1.85 declared and CI-tested on 3 platforms.
- Residue: `version = "0.17"` pin (README.md:47, release-checklist item N2); `AppShell` row (README.md:332, N5, cosmetic deferred). "Minor cosmetic issues only" — A per the scale.

**2. Examples — A** (was A-)
- 91 examples, 17,828 lines, progressive from `counter_app` through `component_showcase` (889) and `beautiful_dashboard` (622). All compile under `--all-features`.
- Component coverage 73/73 (100%). Patterns: focus (`focus_manager`), testing (`test_harness`), async (`async_counter`), composition (`file_manager`, `log_explorer`, `chat_client`), production shape (`production_app`).
- Post-#511 state verified: 22 `use ratatui::` lines, all concrete widgets the prelude deliberately excludes. This is correct layering, not leakage — a user learns exactly one thing: envision components + raw ratatui widgets coexist.

**3. Documentation — A** (held)
- `#![warn(missing_docs)]` enforced; type docs 492/492 (100%); module docs 115/124 (93% — all 9 missing are internal `tests/mod.rs`, invisible to consumers).
- Doc tests on 1777/1777 public fns. 232 `# Examples`, 33 `# Errors`, 15 `# Panics` sections. 28 `no_run`/`ignore` fences, previously sampled as legitimate (terminal-mode runtimes, async harness examples).
- docs/CHOOSING.md component-selection guide; architecture diagram in README.

### Group 2: API Design — GPA 3.88

**4. Consistency & Symmetry — A-** (held)
- `selected_item()` canonical across 16 list-like components; accessor symmetry 0 gaps ("All setters have matching getters" per tool); Debug/Clone/Default/PartialEq on 73/73 State types.
- Builders on 71/74 components; data_grid/form/histogram construct through other idioms.
- Held at A- for the scheduled residue: 17 undeprecated `selected()` aliases (Cadence D), `set_selected_index`/`is_checked`/`label_text` outliers (N3), `restore_terminal`→`restore` rename (N4, the tool's one REEXPORT_GAP).

**5. Modularity & Composability — A-** (held)
- 14 feature flags with component-group granularity; dedicated "No Default Features" CI job proves the opt-out path.
- Dependency leakage: 6 public signatures reference `ratatui::` types (src/backend/cell/mod.rs:69 interop; src/component/context.rs:167 escape hatch; src/component/status_bar/item.rs:420/439/470/490 style params) — deliberate, but the status_bar four could take crate-re-exported paths.
- tokio/tokio-stream/tokio-util/async-stream/futures-util unconditional — the one real "pay for what you don't use" residue; architectural stance, unchanged.

**6. Usability & Ergonomics — A** (held)
- `use envision::prelude::*` covers App/Command/Runtime, all components, input, theme, layout/style; typical app adds only concrete ratatui widgets.
- `RuntimeBuilder::with_args` typestate (sealed `OptionalArgs`) converts a former runtime panic into a compile error, locked by trybuild.
- Edge cases covered by proptest and stress suites.

**7. Complexity Hiding — A** (held)
- Finding #8 re-verified: `message_sender()` returns first-party `MessageSender<A::Message>` (src/harness/app_harness/mod.rs:272); newtype at src/harness/message_sender.rs:42 with `into_inner()` for tokio-native needs; first-party error types.
- Prelude ships first-party `layout`/`style` types; `ratatui::text::{Line, Span, Text}` + `{StatefulWidget, Widget}` re-exports are labeled interop.
- Zero `#[cfg(test)]`-gated non-module items in src/harness/ (tool-verified); `test-utils` feature serves downstream crates.

**8. Type Safety & Errors — A** (held)
- `EnvisionError` with matchable `Io`/`Render`/`Config`/`Subscription`/`Other` variants; trybuild compile-fail suite; `SortKey` enum over stringly-typed comparators; 0 unsafe.

### Group 3: Engineering Quality — GPA 4.00

**9. Algorithms & Data Structures — A-** (held)
- Alloc-tracking bench suite (benches/memory.rs) observes allocation regressions on 1000-item views and 10K state creation.
- compact_str residue: `CompactString` in only src/component/cell.rs and src/backend/cell/mod.rs — sporadic adoption for a crate-wide dep; commit-or-drop decision remains pending (known deferral).
- No O(n)-in-render red flags.

**10. Smart Library Usage — A+** (held)
- 0 unsafe blocks, 0 clippy suppressions across 440 files / 195,191 lines, clippy clean under `--all-features`. Every heavy dep (serde, serde_json, tracing, arboard, pulldown-cmark, regex) optional and gated. 3-platform × 2-toolchain CI.

**11. Performance & Benchmarking — A** (held)
- 6 criterion suites, 1,754 lines: backend, runtime, component view (100/1000 items × 2 sizes), events, memory allocs, 10K-row sort. CI runs benches with criterion report publishing.

### Group 4: Testing — GPA 4.00

**12. Unit Testing — A-** (held)
- 7,306 lib tests; 4,316 component tests (4,113 unit + 203 snapshot). Distribution uneven (box_plot 112 vs conversation_view/log_correlation/log_viewer/multi_progress at 14); snapshot coverage concentrated in ~20/74 components (N7, scheduled v0.18 verification).

**13. Integration & E2E — A** (held)
- 11 files, 5,933 lines, 7,466 tests: multi-component, async, stress (10K items), proptest ×2, serialization round-trips, trybuild, `with_args` wiring. AppHarness exercised in integration tests and examples/test_harness.rs.

**14. Doc Test Coverage — A+** (held)
- 2,591 doc tests, 100.0% of 1,777 public fns. The crate's standout asset.

### Group 5: Architecture — GPA 3.90

**15. Solving Pain Points — A** (held)
- Headless testing (CaptureBackend/AppHarness/DualBackend) delivers full input→update→render without a TTY; 6 built-in themes with public `Theme`/`Palette`/`NamedColor`; widget annotations for accessibility/testability metadata.

**16. Code Organization — A-** (was B+)
- The active-document violation is resolved: CHANGELOG.md 356, MIGRATION.md 224, archives linked. Source discipline: 0 of 548 `.rs` files over 1000 lines.
- Held at A- rather than A: lib.rs re-exports 522 items into a flat namespace (organized in labeled blocks, module paths still available, but large); 5 files sit at 980–997 lines with near-zero headroom (N6). Frozen legacy archives at 1124/1048 lines noted as an accepted, prescribed resolution (footnote finding 9), not graded against.

**17. Extensibility — A** (held)
- Custom `Component` impls get FocusManager/harness/annotation support free; `Subscription` trait + Debounce/Filter/Throttle/Take/Mapped combinators user-implementable; generic `Overlay` trait; disciplined CHANGELOG with structured `[Unreleased]` and Known Deferred Findings transparency.

### Group 6: Missing Pieces — GPA 3.85

**18. Feature Flags — A** — Component-group opt-outs, all heavy deps gated, README-documented, no-default-features CI job. Gap: no tokio opt-out (cats 5/25).

**19. Error Infrastructure — A-** — Structured hierarchy + first-party harness errors; 33 `# Errors` sections against a large (mostly infallible) API; no error-handling guide doc.

**20. Logging & Debugging — A-** — `tracing` feature gates 28 call sites (runtime/command/table); not a systematic event→message→update trace. Debug on 73/73 states.

**21. Guides & Migration — A** — MIGRATION.md v0.16→v0.17 covers all 6 breaks (App::init args/builder split, Table sort/cell redesign, FileSortDirection removal, ResourceGauge builder, selected_item() unification, MessageSender) with before/after tables; history to v0.14 active, earlier archived with link. CONTRIBUTING.md, SECURITY.md, docs/CHOOSING.md present. Missing: performance-tuning guide.

**22. Advanced Features — A** — Clipboard (arboard/TextArea), undo/redo + history (LineInput), search/filter (SearchableList/LogViewer/EventStream regex), animation (Spinner/Toast), lifecycle hooks.

**23. Serialization — A** — `serialization` default feature, `load_state`, dedicated tests/serialization.rs (320 lines) round-trips.

**24. Security — A-** — SECURITY.md (102 lines); ANSI parsing isolated in src/component/terminal_output/ansi.rs (981 lines) with tests; no dedicated escape-sequence fuzz target (proptest partially compensates).

**25. Ecosystem Integration — A-** — Idiomatic ratatui; DualBackend real+capture; concrete widgets via `RenderContext::render_widget`. Hard-wired to tokio and the pinned ratatui major.

## Scorecard (automated, @ 2a5e5a7)

```
Files over 1000 lines (.rs)                       0  PASS
Accessor symmetry gaps                            0  PASS
Doc test coverage                 100.0% (1777/1777) PASS
Debug/Clone/Default/PartialEq on State        73/73  PASS (x4)
Unsafe blocks                                     0  PASS
Clippy suppressions                               0  PASS
Result: 9/9 — cargo test/clippy/doc/examples/doc-tests all PASS
```

## Disposition of prior findings

| Finding (2026-07-05 report) | Status |
|---|---|
| #6 selected accessor divergence | CLOSED (re-verified; alias deprecation remains Cadence D) |
| #8 tokio Sender leakage | CLOSED (re-verified: MessageSender<M> newtype intact) |
| CHANGELOG/MIGRATION > 1000 lines | CLOSED (PR #510; 356/224 with linked archives) |
| Redundant example imports | CLOSED (PR #511; 22 remaining imports all legitimate) |
| N1 duplicate README tables | CLOSED (PR #512; single reconciled table) |
| N2 README `version = "0.17"` pin | OPEN — release-checklist item, verify during `/release minor` |
| N3 naming outliers | OPEN — deferred v0.18+ (Cadence D) |
| N4 restore_terminal rename | OPEN — deferred v0.18+ |
| N5 AppShell in README Utility table | OPEN — deferred, ride with next README pass |
| N6 5 files at 980–997 lines | OPEN — watch, v0.18+ |
| N7 snapshot concentration ~20/74 | OPEN — watch, v0.18+ |
| compact_str sporadic adoption | OPEN — commit-or-drop decision pending |

## New findings this audit

**None.** The only new observation is footnote-severity: the `-legacy.md` archives created by PR #510 themselves exceed 1000 lines (1124/1048) — an accepted consequence of the resolution the prior audit itself prescribed, on frozen append-never files outside the scorecard's `.rs` scope. No action recommended.

## Release recommendation

**SHIP v0.17.0.** All release-gating findings are closed and verified at `2a5e5a7`; the full suite (17,363 tests), clippy, docs, and examples are green; every remaining weakness is documented, cosmetic, and scheduled. One mandatory step during `/release minor`: after the version bump, confirm README.md:47 (`version = "0.17"`) resolves against the published crate before `cargo publish` (retires N2).
